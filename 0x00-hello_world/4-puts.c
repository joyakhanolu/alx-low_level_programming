#include <stdio.h>

/**
 *#include <stdio.h> - generates output defined in stdio.h>
 *
 * int main()- the main entry point to the program.
 *
 * return(0) - terminates the main function and returns the value 0 to the calling process(0-executed successfully)
 */

int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
