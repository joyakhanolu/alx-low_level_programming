#include "main.h"

/**
 *  mul - Entry point
 * Description:  A function that multiplies two integers.
 * @a: the first integer value it receives ..
 * @b: the second integer value it receives
 * Return: Returns product of @a and @b
 */

int mul(int a, int b)
{
return (a * b);
}
